# Repository workflow

- When the owner asks to "commit and push" completed Vantage work, treat that as
  authorization to merge the committed branch into `main` and push `main` too,
  unless the owner explicitly requests a feature-branch-only or pull-request
  workflow.
- Verify that the remote branch and `main` resolve to the expected commit before
  reporting completion.
